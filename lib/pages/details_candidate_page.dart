import 'package:flutter/material.dart';

class DetailsCandidatePage extends StatelessWidget {
  const DetailsCandidatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Details candidat(s)'),
      ),

    );
  }
}
